# get the original file name into an object
orig_name <- cedarwax_four_df$file.name
# get the file extension from the file name as some wav and mp3 files, need to separate so cna run regex on file name
file_type_ext <- tools::file_ext(orig_name)
# have a look
orig_name
file_type_ext
# run regex on file name to get filename into format can copy all files succesfully
# onyl take upper , lower case alpha and numeric characters, underscore and hyphen, nothing else
# any forbidden character replace with an underscore i.e. no brackets, spaces, parenthesis, < >, slahes, commas etc
# re-attache the file extension .wav and mp3
regexd_filename <- gsub("[^A-Za-z0-9_-]", "_", tools::file_path_sans_ext(orig_name))
# have to also put character limit on file names as some are too big and nto copying
max_len <- 40
# modify the original filename then re-attach the extension
regexd_charlim_filename <- substr(regexd_filename, 1, max_len)
regexd_charlim_ext_filename <- paste0(regexd_charlim_filename, ".", file_type_ext)
regexd_charlim_ext_filename
#Amend this to reflect your folder structure, if it doesn't exist create the folder
datalocation <- file.path("./dwnlod")
if (!dir.exists(datalocation)) dir.create(datalocation)
# where to copy to with amended file name
dest_path <- file.path(datalocation,regexd_charlim_ext_filename)
#download the aaudio files
download.file(url = file_url, destfile = dest_path, mode = "wb")
# modified this from provided to make it easier for me to track the variables, dest_path to file_name
# re-used dest path to include a sib fodler to place the files in
file_url <- cedarwax_four_df$audio_file
file_name <- cedarwax_four_df$recording_id
# some file names have characters which are not handled well when copying, get failed copies
# modifying approch here to modify the fle name and then copy
# get the original file name into an object
orig_name <- cedarwax_four_df$file.name
# get the file extension from the file name as some wav and mp3 files, need to separate so cna run regex on file name
file_type_ext <- tools::file_ext(orig_name)
# have a look
orig_name
file_type_ext
# run regex on file name to get filename into format can copy all files succesfully
# onyl take upper , lower case alpha and numeric characters, underscore and hyphen, nothing else
# any forbidden character replace with an underscore i.e. no brackets, spaces, parenthesis, < >, slahes, commas etc
# re-attache the file extension .wav and mp3
regexd_filename <- gsub("[^A-Za-z0-9_-]", "_", tools::file_path_sans_ext(orig_name))
# have to also put character limit on file names as some are too big and nto copying
max_len <- 40
# modify the original filename then re-attach the extension
regexd_charlim_filename <- substr(regexd_filename, 1, max_len)
regexd_charlim_ext_filename <- paste0(regexd_charlim_filename, ".", file_type_ext)
regexd_charlim_ext_filename
#Amend this to reflect your folder structure, if it doesn't exist create the folder
datalocation <- file.path("./dwnlod")
if (!dir.exists(datalocation)) dir.create(datalocation)
# where to copy to with amended file name
dest_path <- file.path(datalocation,regexd_charlim_ext_filename)
#download the aaudio files
download.file(url = file_url, destfile = dest_path, mode = "wb")
library(ggplot2)
library(maps)
library(dplyr)
library(tuneR)
audio_file_list <- list.files("./dwnld")
audio_file_list
audio_file_list <- list.files("./dwnld", full.names - TRUE)
audio_file_list <- list.files("./dwnld", full.names - TRUE)
audio_file_list <- list.files("./dwnld", full.names = TRUE)
audio_file_list
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
for file in audio_file_list
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
for f in audio_file_list
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
for (f in audio_file_list)
{
file_type_ext <- tools::file_ext(f)
file_type_ext
}
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
for (f in audio_file_list)
{
file_type_ext <- tools::file_ext(f)
print(file_type_ext)
}
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
num_mp3 <- 0
num_wav <- 0
for (f in audio_file_list)
{
file_type_ext <- tools::file_ext(f)
print(type(file_type_ext))
# count_i <- nrow(temp)
# sum_four_voc_types <- sum_four_voc_types + count_i
# print(file_type_ext)
#
}
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
num_mp3 <- 0
num_wav <- 0
for (f in audio_file_list)
{
file_type_ext <- tools::file_ext(f)
print(class(file_type_ext))
# count_i <- nrow(temp)
# sum_four_voc_types <- sum_four_voc_types + count_i
# print(file_type_ext)
#
}
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
num_mp3 <- sum(tolower(file_types) == "mp3")
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
num_mp3 <- sum(tolower(audio_file_list) == "mp3")
num_wav <- sum(tolower(audio_file_list) == "wav")
num_mp3
num_wav
for (f in audio_file_list)
{
file_type_ext <- tools::file_ext(f)
print(class(file_type_ext))
# count_i <- nrow(temp)
# sum_four_voc_types <- sum_four_voc_types + count_i
# print(file_type_ext)
#
}
dir.exists("./dwnld")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(f)
num_mp3 <- sum(tolower(file_type_ext) == "mp3")
num_wav <- sum(tolower(file_type_ext) == "wav")
num_mp3
num_wav
for (f in audio_file_list)
{
file_type_ext <- tools::file_ext(f)
print(class(file_type_ext))
# count_i <- nrow(temp)
# sum_four_voc_types <- sum_four_voc_types + count_i
# print(file_type_ext)
#
}
# ref > https://stackoverflow.com/questions/7779037/extract-file-extension-from-file-path
# modified this from what was provided provided to make it easier for me to track the variables, i.e. changed est_path to file_name
# re-used dest path to include a sub folder to place the files in
file_url <- cedarwax_four_df$audio_file
file_name <- cedarwax_four_df$recording_id
# some file names have characters which are not handled well when copying, get failed copies
# modifying approch here to modify the fle name and then copy
# get the original file name into an object
orig_name <- cedarwax_four_df$file.name
# get the file extension from the file name as some wav and mp3 files, need to separate so cna run regex on file name
file_type_ext <- tools::file_ext(orig_name)
# have a look
orig_name
file_type_ext
# run regex on file name to get filename into format can copy all files succesfully
# onyl take upper , lower case alpha and numeric characters, underscore and hyphen, nothing else
# any forbidden character replace with an underscore i.e. no brackets, spaces, parenthesis, < >, slahes, commas etc
# re-attache the file extension .wav and mp3
regexd_filename <- gsub("[^A-Za-z0-9_-]", "_", tools::file_path_sans_ext(orig_name))
# have to also put character limit on file names as some are too big and nto copying
max_len <- 40
# modify the original filename then re-attach the extension
regexd_charlim_filename <- substr(regexd_filename, 1, max_len)
regexd_charlim_ext_filename <- paste0(regexd_charlim_filename, ".", file_type_ext)
regexd_charlim_ext_filename <- tolower(regexd_charlim_ext_filename)
#Amend this to reflect your folder structure, if it doesn't exist create the folder
datalocation <- file.path("./dwnlod")
if (!dir.exists(datalocation)) dir.create(datalocation)
# where to copy to with amended file name
dest_path <- file.path(datalocation,regexd_charlim_ext_filename)
#download the aaudio files
download.file(url = file_url, destfile = dest_path, mode = "wb")
# ref > https://stackoverflow.com/questions/7779037/extract-file-extension-from-file-path
# modified this from what was provided provided to make it easier for me to track the variables, i.e. changed est_path to file_name
# re-used dest path to include a sub folder to place the files in
file_url <- cedarwax_four_df$audio_file
file_name <- cedarwax_four_df$recording_id
# some file names have characters which are not handled well when copying, get failed copies
# modifying approch here to modify the fle name and then copy
# get the original file name into an object
orig_name <- cedarwax_four_df$file.name
# get the file extension from the file name as some wav and mp3 files, need to separate so cna run regex on file name
file_type_ext <- tools::file_ext(orig_name)
# have a look
orig_name
file_type_ext
# run regex on file name to get filename into format can copy all files succesfully
# onyl take upper , lower case alpha and numeric characters, underscore and hyphen, nothing else
# any forbidden character replace with an underscore i.e. no brackets, spaces, parenthesis, < >, slahes, commas etc
# re-attache the file extension .wav and mp3
regexd_filename <- gsub("[^A-Za-z0-9_-]", "_", tools::file_path_sans_ext(orig_name))
# have to also put character limit on file names as some are too big and nto copying
max_len <- 40
# modify the original filename then re-attach the extension
regexd_charlim_filename <- substr(regexd_filename, 1, max_len)
regexd_charlim_ext_filename <- paste0(regexd_charlim_filename, ".", file_type_ext)
regexd_charlim_ext_filename <- tolower(regexd_charlim_ext_filename)
#Amend this to reflect your folder structure, if it doesn't exist create the folder
datalocation <- file.path("./dwnlod")
if (!dir.exists(datalocation)) dir.create(datalocation)
# where to copy to with amended file name
dest_path <- file.path(datalocation,regexd_charlim_ext_filename)
#download the aaudio files
download.file(url = file_url, destfile = dest_path, mode = "wb")
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(f)
num_mp3 <- sum(file_type_ext) == "mp3")
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(f)
num_mp3 <- sum(file_type_ext) == "mp3"))
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(f)
num_mp3 <- sum((file_type_ext) == "mp3"))
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(f)
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
num_mp3
num_wav
for (f in audio_file_list)
{
file_type_ext <- tools::file_ext(f)
print(class(file_type_ext))
# count_i <- nrow(temp)
# sum_four_voc_types <- sum_four_voc_types + count_i
# print(file_type_ext)
#
}
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(f)
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
num_mp3
num_wav
# for (f in audio_file_list)
# {
#   file_type_ext <- tools::file_ext(f)
#   print(class(file_type_ext))
#
#   # count_i <- nrow(temp)
#   # sum_four_voc_types <- sum_four_voc_types + count_i
#   # print(file_type_ext)
#   #
#
}
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(audio_file_list )
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
num_mp3
num_wav
# for (f in audio_file_list)
# {
#   file_type_ext <- tools::file_ext(f)
#   print(class(file_type_ext))
#
#   # count_i <- nrow(temp)
#   # sum_four_voc_types <- sum_four_voc_types + count_i
#   # print(file_type_ext)
#   #
#
}
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(audio_file_list )
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
print(paste0("You have ", num_mp3, " mp3 files")
print(paste0("You have ", num_wav, " wav files")
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(audio_file_list )
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
print(paste0("You have ", num_mp3, " mp3 files"))
print(paste0("You have ", num_wav, " wav files"))
# for (f in audio_file_list)
# {
#   file_type_ext <- tools::file_ext(f)
#   print(class(file_type_ext))
#
#   # count_i <- nrow(temp)
#   # sum_four_voc_types <- sum_four_voc_types + count_i
#   # print(file_type_ext)
#   #
#
}
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(audio_file_list )
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
print(paste0("You have ", num_mp3, " mp3 files"))
print(paste0("You have ", num_wav, " wav files"))
# for (f in audio_file_list)
# {
#   file_type_ext <- tools::file_ext(f)
#   print(class(file_type_ext))
#
#   # count_i <- nrow(temp)
#   # sum_four_voc_types <- sum_four_voc_types + count_i
#   # print(file_type_ext)
#   #
#
}
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(audio_file_list )
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
print(paste0("You have ", num_mp3, " mp3 files"))
print(paste0("You have ", num_wav, " wav files"))
# for (f in audio_file_list)
# {
#   file_type_ext <- tools::file_ext(f)
#   print(class(file_type_ext))
#
#   # count_i <- nrow(temp)
#   # sum_four_voc_types <- sum_four_voc_types + count_i
#   # print(file_type_ext)
#   #
#
# }
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(audio_file_list )
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
print(paste0("You have ", num_mp3, " mp3 files"))
print(paste0("You have ", num_wav, " wav files"))
for (f in audio_file_list)
{
audio_mp3 <- readmp3(f)
writeWave(audio_mp3, audio_file_list)
print(paste0(" converted ", [i])
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(audio_file_list )
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
print(paste0("You have ", num_mp3, " mp3 files"))
print(paste0("You have ", num_wav, " wav files"))
mp3_files <- audio_file_list(file_type_ext) -- "mp3"
dir.exists("./dwnlod")
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
file_type_ext <- tools::file_ext(audio_file_list )
num_mp3 <- sum((file_type_ext) == "mp3")
num_wav <- sum((file_type_ext) == "wav")
print(paste0("You have ", num_mp3, " mp3 files"))
print(paste0("You have ", num_wav, " wav files"))
mp3_files <- audio_file_list(file_type_ext) == "mp3"
audio_file_list <- list.files("./dwnlod", full.names = TRUE)
audio_file_list
all_audio_file_list <- list.files("./dwnlod", full.names = TRUE)
all_audio_files
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
all_audio_files
mp3_files <- all_audio_files(tools::file_ext(all_audio_files)== "mp3"
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
#all_audio_files
mp3_files <- all_audio_files(tools::file_ext(all_audio_files)== "mp3")
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
#all_audio_files
mp3_files <- all_audio_files[tools::file_ext(all_audio_files)== "mp3"]
print(paste0("You have ", length(mp3_files), " mp3 files"))
wav_files <- all_audio_files[tools::file_ext(all_audio_files)== "wav"]
print(paste0("You have ", length(wav_files), " wav files"))
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
print(paste0("You have ", length(all_audio_files), " audio files in total"))
mp3_files <- all_audio_files[tools::file_ext(all_audio_files)== "mp3"]
print(paste0("You have ", length(mp3_files), " mp3 files"))
wav_files <- all_audio_files[tools::file_ext(all_audio_files)== "wav"]
print(paste0("You have ", length(wav_files), " wav files"))
# dir.exists("./dwnlod")
# get object with all files in the folder and get the numebr of files in the folder, just using length function
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
print(paste0("You have ", length(all_audio_files), " audio files in total"))
# using file_ext create wav and mp3 objects to work on and then again get the length of those objects to get the number of files for each
mp3_files <- all_audio_files[tools::file_ext(all_audio_files)== "mp3"]
print(paste0("You have ", length(mp3_files), " mp3 files"))
wav_files <- all_audio_files[tools::file_ext(all_audio_files)== "wav"]
print(paste0("You have ", length(wav_files), " wav files"))
# read the mp3 files into an audio data object with readmp3
# we have to give the convert writewave fn a file name so we'll create a wav file name to sue dugin conversion
for (file in mp3_files)
{
audio_mp3 <- readMP3(file)
# crate wav file name to use when converting based off the mps filename, will su ebasename to treturn just the filename from the path
mp3_file_name_sans_ext<- tools::file_path_sans_ext(basename(file))
#crate wav filename to sue during conversion
new_wav_file <- file.path(mp3_file_name_sans_ext, ".wav", f)
writeWave(audio_mp3, new_wav_file)
print(paste0(" converted ", f)
}
# dir.exists("./dwnlod")
datalocation <- file.path("./dwnlod")
convered_mp3_location <-<- file.path("./dwnlod/converted_mp3_files")
# dir.exists("./dwnlod")
datalocation <- file.path("./dwnlod")
convered_mp3_location <- file.path("./dwnlod/converted_mp3_files")
if (!dir.exists(convered_mp3_location )) dir.convered_mp3_location)
# dir.exists("./dwnlod")
datalocation <- file.path("./dwnlod")
convered_mp3_location <- file.path("./dwnlod/converted_mp3_files")
if (!dir.exists(convered_mp3_location )) dir.create(convered_mp3_location)
# get object with all files in the folder and get the numebr of files in the folder, just using length function
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
print(paste0("You have ", length(all_audio_files), " audio files in total"))
# using file_ext create wav and mp3 objects to work on and then again get the length of those objects to get the number of files for each
mp3_files <- all_audio_files[tools::file_ext(all_audio_files)== "mp3"]
print(paste0("You have ", length(mp3_files), " mp3 files"))
wav_files <- all_audio_files[tools::file_ext(all_audio_files)== "wav"]
print(paste0("You have ", length(wav_files), " wav files"))
# read the mp3 files into an audio data object with readmp3
# we have to give the convert writewave fn a file name so we'll create a wav file name to sue dugin conversion
for (file in mp3_files)
{
audio_mp3 <- readMP3(file)
# crate wav file name to use when converting based off the mps filename, will su ebasename to treturn just the filename from the path
mp3_file_name_sans_ext<- tools::file_path_sans_ext(basename(file))
#crate wav filename to sue during conversion
new_wav_filename <- file.path(datalocation, paste0(mp3_file_name_sans_ext, ".wav")
writeWave(audio_mp3, new_wav_filename)
# dir.exists("./dwnlod")
datalocation <- file.path("./dwnlod")
convered_mp3_location <- file.path("./dwnlod/converted_mp3_files")
if (!dir.exists(convered_mp3_location )) dir.create(convered_mp3_location)
# get object with all files in the folder and get the numebr of files in the folder, just using length function
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
print(paste0("You have ", length(all_audio_files), " audio files in total"))
# using file_ext create wav and mp3 objects to work on and then again get the length of those objects to get the number of files for each
mp3_files <- all_audio_files[tools::file_ext(all_audio_files)== "mp3"]
print(paste0("You have ", length(mp3_files), " mp3 files"))
wav_files <- all_audio_files[tools::file_ext(all_audio_files)== "wav"]
print(paste0("You have ", length(wav_files), " wav files"))
mp3_conv_count <- 0
# read the mp3 files into an audio data object with readmp3
# we have to give the convert writewave fn a file name so we'll create a wav file name to sue dugin conversion
for (file in mp3_files)
{
audio_mp3 <- readMP3(file)
# crate wav file name to use when converting based off the mps filename, will su ebasename to treturn just the filename from the path
mp3_file_name_sans_ext<- tools::file_path_sans_ext(basename(file))
#crate wav filename to sue during conversion
new_wav_filename <- file.path(datalocation, paste0(mp3_file_name_sans_ext, ".wav")
writeWave(audio_mp3, new_wav_filename)
# dir.exists("./dwnlod")
datalocation <- file.path("./dwnlod")
convered_mp3_location <- file.path("./dwnlod/converted_mp3_files")
if (!dir.exists(convered_mp3_location )) dir.create(convered_mp3_location)
# get object with all files in the folder and get the numebr of files in the folder, just using length function
all_audio_files <- list.files("./dwnlod", full.names = TRUE)
print(paste0("You have ", length(all_audio_files), " audio files in total"))
# using file_ext create wav and mp3 objects to work on and then again get the length of those objects to get the number of files for each
mp3_files <- all_audio_files[tools::file_ext(all_audio_files)== "mp3"]
print(paste0("You have ", length(mp3_files), " mp3 files"))
wav_files <- all_audio_files[tools::file_ext(all_audio_files)== "wav"]
print(paste0("You have ", length(wav_files), " wav files"))
# crate a coutnign object to sue int he loop t get totals converted
mp3_conv_count <- 0
# read the mp3 files into an audio data object with readmp3
# we have to give the convert writewave fn a file name so we'll create a wav file name to sue dugin conversion
for (file in mp3_files)
{
audio_mp3 <- readMP3(file)
# crate wav file name to use when converting based off the mps filename, will su ebasename to treturn just the filename from the path
mp3_file_name_sans_ext<- tools::file_path_sans_ext(basename(file))
#crate wav filename to sue during conversion
new_wav_filename <- file.path(datalocation, paste0(mp3_file_name_sans_ext, ".wav"))
writeWave(audio_mp3, new_wav_filename)
print(paste0(" converted ", basename(file)))
mp3_conv_count <- mp3_conv_count +1
}
print(paste0("Converted ", mp3_conv_count, "mp3 files"))
